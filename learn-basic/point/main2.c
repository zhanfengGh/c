#include "redirect_ptr.h"
#include <stdio.h>

int main(int argc, const char *argv[])
{
    printf("%s, %s\n", get_a_day2(0), get_a_day2(1));
    printf("%s, %s\n", get_a_day3(0), get_a_day3(1));
    return 0;
}
